<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jyx.mapper.CategoryMapperCustom" >
  <resultMap id="myCategoryVO" type="com.jyx.pojo.vo.CategoryVO">
    <id column="id" property="id"/>
    <result column="name" property="name"/>
    <result column="type" property="type"/>
    <result column="fatherId" property="fatherId"/>
    <!--
      collection 用于定义关联的list集合类型的封装规则
      property：用于对应三级分类的list的属性名
      ofType：集合的类型，三级分类VO
    -->
    <collection property="subCatList" ofType="com.jyx.pojo.vo.SubCategoryVO">
      <id column="subId" property="subId"/>
      <result column="subType" property="subType"/>
      <result column="subName" property="subName"/>
      <result column="subFatherId" property="subFatherId"/>
    </collection>
  </resultMap>
  <select id="getSubCatList" resultMap="myCategoryVO" parameterType="int">
      SELECT
	    f.id as id,
	    f.type as type,
	    f.`name` as `name`,
	    f.father_id as fatherId,
	    c.id as subId,
	    c.type as subType,
	    c.`name` as subName,
	    c.father_id as subFatherId
      FROM
        category f
      left join
        category c
      on
        f.id = c.father_id
      WHERE
        f.father_id = #{rootCatId}
  </select>
</mapper>